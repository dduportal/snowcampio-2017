version: '2'

services:
    jenkins:
        build: ./jenkins/
        restart: unless-stopped
        ports:
            - "8080:8080"
            - "50000:50000"
        environment:
            - JENKINS_OPTS=--prefix=/jenkins
